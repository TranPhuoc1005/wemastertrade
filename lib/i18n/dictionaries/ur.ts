import type { PartialDictionary } from "../types";

const dictionary = {
    common: {
        header: {
            announcement: `اعلان`,
        },
    },
} satisfies PartialDictionary;

export default dictionary;
